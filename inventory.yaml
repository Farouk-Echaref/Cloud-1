---
all:
  hosts:
    server1:
      ansible_host: "{{ secrets.server1.host }}"
      ansible_user: "{{ secrets.server1.user }}"
      ansible_ssh_private_key_file: "{{ secrets.server1.ssh_key_path }}"
      ansible_python_interpreter: "/usr/bin/python3"
      # ansible_become_pass: "{{ secrets.server1.password }}"
    server2:
      ansible_host: "{{ secrets.server2.host }}"
      ansible_user: "{{ secrets.server2.user }}"
      ansible_ssh_private_key_file: "{{ secrets.server2.ssh_key_path }}"
      ansible_python_interpreter: "/usr/bin/python3"
    server3:
      ansible_host: "{{ secrets.server3.host }}"
      ansible_user: "{{ secrets.server3.user }}"
      ansible_ssh_private_key_file: "{{ secrets.server3.ssh_key_path }}"
      ansible_python_interpreter: "/usr/bin/python3"
    server4:
      ansible_host: "{{ secrets.server4.host }}"
      ansible_user: "{{ secrets.server4.user }}"
      ansible_ssh_private_key_file: "{{ secrets.server4.ssh_key_path }}"
      ansible_python_interpreter: "/usr/bin/python3"
