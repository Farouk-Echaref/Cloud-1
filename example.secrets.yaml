secrets:
  server1:
    host: "your_server1_host"
    user: "machine_user"            # Replace with your machine's user
    ssh_key_path: "/path/to/your/private_key"  # Replace with the path to your private key
  server2:
    host: "your_server2_host"
    user: "machine_user"            # Replace with your machine's user
    ssh_key_path: "/path/to/your/private_key"  # Replace with the path to your private key
  server3:
    host: "your_server3_host"
    user: "machine_user"            # Replace with your machine's user
    ssh_key_path: "/path/to/your/private_key"  # Replace with the path to your private key
